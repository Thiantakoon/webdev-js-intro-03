A number data type added to a string returns a string because + operator appends one string onto another
whencomparing values of different types ,it can result in creating bugs
the operator||.Itmeans "or" but console.log (2||1 Output is 2)
i use the modulus to get the remainder